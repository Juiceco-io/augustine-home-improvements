name: Deploy

on:
  push:
    branches: [dev, qa, main]

# Minimal permissions — only what each job needs
permissions:
  id-token: write   # Required for OIDC token
  contents: read

env:
  AWS_REGION: us-east-1
  TF_DIR: infrastructure

jobs:
  # ─────────────────────────────────────────────
  # Job 0: Resolve GitHub Environment name + AWS config
  # ─────────────────────────────────────────────
  set-environment:
    name: Set Environment
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.set-env.outputs.environment }}
      account_id: ${{ steps.aws-config.outputs.account_id }}
      deployer_role: ${{ steps.aws-config.outputs.deployer_role }}
      state_role: ${{ steps.aws-config.outputs.state_role }}
      cloudfront_aliases_json: ${{ steps.aws-config.outputs.cloudfront_aliases_json }}
    steps:
      - id: set-env
        name: Map branch → environment
        run: |
          case "${{ github.ref_name }}" in
            dev)  echo "environment=dev"  >> $GITHUB_OUTPUT ;;
            qa)   echo "environment=qa"   >> $GITHUB_OUTPUT ;;
            main) echo "environment=prod" >> $GITHUB_OUTPUT ;;
            *)
              echo "Unknown branch: ${{ github.ref_name }}" >&2
              exit 1
              ;;
          esac

      - name: Set AWS config
        id: aws-config
        run: |
          # NOTE: Augustine Home Improvements AWS account IDs are not yet
          # provisioned via the platform OU. Replace the TODO placeholders below
          # with the actual account IDs once aft-account-requests provisions the
          # augustine-home-improvements OU. Deployer roles follow the platform
          # convention: arn:aws:iam::<ACCOUNT_ID>:role/github-actions-deployer
          #
          # The state_role (389813258758) is the shared terraform-state account;
          # its trust policy will also need to be updated to include
          # augustine-home-improvements in the OIDC subject match.
          case "${{ github.ref_name }}" in
            dev)
              echo "account_id=TODO_AUGUSTINE_DEV_ACCOUNT_ID" >> $GITHUB_OUTPUT
              echo "deployer_role=arn:aws:iam::TODO_AUGUSTINE_DEV_ACCOUNT_ID:role/github-actions-deployer" >> $GITHUB_OUTPUT
              echo 'cloudfront_aliases_json=[]' >> $GITHUB_OUTPUT
              ;;
            qa)
              echo "account_id=TODO_AUGUSTINE_QA_ACCOUNT_ID" >> $GITHUB_OUTPUT
              echo "deployer_role=arn:aws:iam::TODO_AUGUSTINE_QA_ACCOUNT_ID:role/github-actions-deployer" >> $GITHUB_OUTPUT
              echo 'cloudfront_aliases_json=[]' >> $GITHUB_OUTPUT
              ;;
            main)
              echo "account_id=TODO_AUGUSTINE_PROD_ACCOUNT_ID" >> $GITHUB_OUTPUT
              echo "deployer_role=arn:aws:iam::TODO_AUGUSTINE_PROD_ACCOUNT_ID:role/github-actions-deployer" >> $GITHUB_OUTPUT
              # Replace with actual domain aliases once the custom domain is confirmed,
              # e.g.: '["augustinehomeimprovements.com","www.augustinehomeimprovements.com"]'
              echo 'cloudfront_aliases_json=[]' >> $GITHUB_OUTPUT
              ;;
          esac
          # Shared Terraform state account — same for all apps in the platform
          echo "state_role=arn:aws:iam::389813258758:role/github-actions-terraform-state" >> $GITHUB_OUTPUT

  # ─────────────────────────────────────────────
  # Job 1: Provision infrastructure with Terraform
  # ─────────────────────────────────────────────
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: set-environment
    environment: ${{ needs.set-environment.outputs.environment }}
    outputs:
      cloudfront_distribution_id: ${{ steps.tf-output.outputs.cloudfront_distribution_id }}
      s3_bucket_name: ${{ steps.tf-output.outputs.s3_bucket_name }}
      cloudfront_domain_name: ${{ steps.tf-output.outputs.cloudfront_domain_name }}
      contact_api_url: ${{ steps.tf-output.outputs.contact_api_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (Terraform state)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.set-environment.outputs.state_role }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.7"
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform init \
            -backend-config="key=augustine-home-improvements/${{ needs.set-environment.outputs.environment }}/terraform.tfstate"

      - name: Terraform Plan
        working-directory: ${{ env.TF_DIR }}
        env:
          TF_VAR_contact_from_email: ${{ vars.CONTACT_FROM_EMAIL }}
          TF_VAR_contact_to_email: ${{ vars.CONTACT_TO_EMAIL }}
          TF_VAR_contact_email_domain: ${{ vars.CONTACT_EMAIL_DOMAIN }}
        run: |
          terraform plan \
            -var="environment=${{ needs.set-environment.outputs.environment }}" \
            -var="deployer_role_arn=${{ needs.set-environment.outputs.deployer_role }}" \
            -var='cloudfront_aliases=${{ needs.set-environment.outputs.cloudfront_aliases_json }}' \
            -out=tfplan

      - name: Terraform Apply
        working-directory: ${{ env.TF_DIR }}
        run: terraform apply -auto-approve tfplan

      - name: Get Terraform Outputs
        id: tf-output
        working-directory: ${{ env.TF_DIR }}
        run: |
          CF_ID=$(terraform output -raw cloudfront_distribution_id)
          BUCKET=$(terraform output -raw s3_bucket_name)
          CF_DOMAIN=$(terraform output -raw cloudfront_domain_name)
          CONTACT_API_URL=$(terraform output -raw contact_api_url)
          echo "cloudfront_distribution_id=$CF_ID" >> "$GITHUB_OUTPUT"
          echo "s3_bucket_name=$BUCKET" >> "$GITHUB_OUTPUT"
          echo "cloudfront_domain_name=$CF_DOMAIN" >> "$GITHUB_OUTPUT"
          echo "contact_api_url=$CONTACT_API_URL" >> "$GITHUB_OUTPUT"
          echo "::notice title=CloudFront URL::$CF_DOMAIN"

  # ─────────────────────────────────────────────
  # Job 2: Build Next.js static export and deploy to S3
  # ─────────────────────────────────────────────
  build-and-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    needs: [set-environment, terraform-apply]
    environment: ${{ needs.set-environment.outputs.environment }}
    env:
      CF_DISTRIBUTION_ID: ${{ needs.terraform-apply.outputs.cloudfront_distribution_id }}
      S3_BUCKET: ${{ needs.terraform-apply.outputs.s3_bucket_name }}
      IAM_ROLE: ${{ needs.set-environment.outputs.deployer_role }}
      NEXT_PUBLIC_CONTACT_API_URL: ${{ needs.terraform-apply.outputs.contact_api_url }}
      NEXT_PUBLIC_APP_URL: https://${{ needs.terraform-apply.outputs.cloudfront_domain_name }}
      NEXT_PUBLIC_GA_MEASUREMENT_ID: ${{ vars.NEXT_PUBLIC_GA_MEASUREMENT_ID }}
      NEXT_PUBLIC_APP_VERSION: ${{ github.sha }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build static export
        run: npm run build

      - name: Configure AWS credentials (Deploy)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.IAM_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync static assets to S3
        run: |
          aws s3 sync ./out s3://${{ env.S3_BUCKET }} \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "*.html" \
            --exclude "*.json" \
            --exclude "*.txt" \
            --exclude "*.xml"

      - name: Sync HTML and manifests to S3
        run: |
          aws s3 sync ./out s3://${{ env.S3_BUCKET }} \
            --cache-control "public, max-age=0, must-revalidate" \
            --include "*.html" \
            --include "*.json" \
            --include "*.txt" \
            --include "*.xml"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CF_DISTRIBUTION_ID }} \
            --paths "/*"

      - name: Print deployment URL
        run: |
          echo "✅ Deployed to: https://${{ needs.terraform-apply.outputs.cloudfront_domain_name }}"
